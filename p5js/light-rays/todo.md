- [ ] with insertion enabled, parabolic mirror bugs out and gets weird incorrect reflections. same thing happens with circle if you pulse 3/4 of a radius from the center. not sure if insertion bug, reflection bug, or both. could be related to new special rules like no insert if would cause collision, no insert if at-all-different optic lists. not sure if this behavior existed before those changes. if you turn on dots, you'll see that when this happens, there is usually also a very distant connected sibling pair that doesn't get interpolated. might want to make debug tooling like a stepper, named mirrors, and ray info on hover like highlighting siblings and showing mirror name list.
- [ ] maybe connect siblings if they have the same set of optics but permuted differently? would fix the bug in the box where bouncing of top then right disconnects from sibling that bounced right then top
- [x] if siblings are an edit away but far apart, don't connect